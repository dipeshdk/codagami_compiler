void printf(char* p, int d);
char* str = "%d\n";

int main() {
    int a = 3, b = 3, c = 0;
    char p, q, r;

    if (a == b) {
        printf(str, 584654646);
        if (c == 0) {
            p = 'a';
            printf(str, p);
        } else if (c == 1) {
            q = 'a';
            printf(str, q);
        } else {
            r = 'b';
            printf(str, r);
        }
    }

    if (!c) {
        printf(str, 679896);
        if (a) {
            printf(str, 587685);
            if (a >= p * c + b) {
                printf(str, 3490930);
                return 0;
            } else {
                a == p* c + b + 1;
                printf(str, a);
            }
        }
    }
    return 0;
}